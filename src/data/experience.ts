import type { FoundationContent } from '../types/portfolio';

export const experienceContent: FoundationContent = {
  description: 'A foundation page for the verified professional timeline and role-specific contributions.',
};

