import type { FoundationContent } from '../types/portfolio';

export const credentialsContent: FoundationContent = {
  description: 'A foundation page for verified professional certifications and continuing development.',
};

